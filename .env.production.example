# ===========================================
# Seisei BizNexus - Production Environment
# ===========================================
# Copy this file to .env on your production server
# and fill in the actual values

# Database (PostgreSQL)
DATABASE_URL="postgresql://seisei:CHANGE_THIS_PASSWORD@postgres:5432/seisei_erp"
POSTGRES_USER=seisei
POSTGRES_PASSWORD=CHANGE_THIS_PASSWORD
POSTGRES_DB=seisei_erp

# App Configuration
APP_PORT=9527
NODE_ENV=production

# Security - IMPORTANT: Generate secure random values!
# Generate JWT_SECRET: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_RANDOM_64_CHAR_STRING

# Generate ENCRYPTION_KEY: openssl rand -hex 16
ENCRYPTION_KEY=CHANGE_THIS_32_CHAR_KEY_HERE!!

# Odoo Configuration (your Odoo server)
DEFAULT_ODOO_URL=https://your-odoo-server.com
DEFAULT_ODOO_DB=your_odoo_database

# ===========================================
# Stripe Configuration (Payment Processing)
# ===========================================
# Get these from: https://dashboard.stripe.com/apikeys

# Secret Key (server-side only, never expose to client)
STRIPE_SECRET_KEY=sk_live_xxxxxxxxxxxxxxxxxxxxx

# Publishable Key (safe for client-side)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_xxxxxxxxxxxxxxxxxxxxx

# Webhook Secret (from Stripe Dashboard -> Webhooks)
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxx

# App URL (for Stripe redirects)
NEXT_PUBLIC_APP_URL=https://erp.seisei.tokyo

# ===========================================
# Cron Job Configuration
# ===========================================
# Secret for authenticating cron job requests
# Generate with: openssl rand -base64 32
CRON_SECRET=CHANGE_THIS_TO_RANDOM_SECRET

# ===========================================
# Email Configuration
# ===========================================
# Provider: resend, smtp, or console (default: console)
EMAIL_PROVIDER=resend

# Resend (recommended)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxx

# Or SMTP
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-smtp-user
# SMTP_PASS=your-smtp-password

# Email sender
EMAIL_FROM=Seisei BizNexus <noreply@seisei.tokyo>

# ===========================================
# Odoo 19 Configuration
# ===========================================
ODOO19_URL=http://your-odoo-server:8069
ODOO19_DB=your_database
ODOO19_USERNAME=admin
ODOO19_PASSWORD=your_password

# Odoo Webhook Secret (for receiving events from Odoo)
ODOO_WEBHOOK_SECRET=your_odoo_webhook_secret
