# Central OCR Service Configuration

# Gemini API Key (required - managed centrally, NOT exposed to tenants)
GEMINI_API_KEY=your-gemini-api-key

# Service authentication key (shared with Odoo 18 instances)
OCR_SERVICE_KEY=your-secure-service-key

# Database password
OCR_DB_PASSWORD=secure-db-password

# Free quota per tenant per month (default: 30)
OCR_FREE_QUOTA=30

# Price per image in JPY after free quota (default: 20)
OCR_PRICE_PER_IMAGE=20
