# Edge Nginx Router Environment Configuration
# This stack has minimal env vars as most config is in default.conf

# Nginx image version (optional override)
# NGINX_VERSION=1.27-alpine

# Log level (optional)
# NGINX_LOG_LEVEL=warn
